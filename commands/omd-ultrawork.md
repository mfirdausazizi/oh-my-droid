---
description: Maximum parallel execution with multiple custom droids
---

Execute the following task in ultrawork mode with maximum parallelism:

$ARGUMENTS

Use the ultrawork skill. Identify independent subtasks, spawn custom droids (oracle, librarian, architect, executor) in parallel via Task tool with run_in_background=true, and synthesize results.
