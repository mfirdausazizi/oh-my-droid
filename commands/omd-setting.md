---
description: Toggle smart model assignment for custom droids
argument-hint: <on|off|status>
---

Manage smart model assignment runtime bridge.

1. Run:
   - `omd setting smart-model-assignment $ARGUMENTS`
2. Return the command output.
3. If argument is empty, use `status`.
